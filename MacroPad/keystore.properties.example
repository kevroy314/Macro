# Copy to keystore.properties and fill in. That file is git-ignored.
#
# These credentials sign releases, and the in-app updater installs anything signed
# with this key. Do not commit either this filled-in file or the keystore itself.
storeFile=../macropad-release-key.jks
storePassword=
keyAlias=macropad
keyPassword=
